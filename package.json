{
  "name": "almma-openai-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs --config config.json --env-file .env",
    "check": "node --check server.mjs && node server.mjs --check-config"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "undici": "^6.25.0"
  }
}
